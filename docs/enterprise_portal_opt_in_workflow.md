# Opt-in workflow

Not enrolled → review information → select goals → accept consents (none preselected) → submit → eligibility check → pending → active / changes_requested / declined.

Routes: `/enterprise/join.php`, `/enterprise/membership_status.php`, `/enterprise/onboarding.php`.
