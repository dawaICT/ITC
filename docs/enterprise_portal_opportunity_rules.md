# Opportunity rules

Types: professional_skill, employment_profile, service, product, innovation, business_idea, investment_opportunity.

Statuses: draft → submitted → changes_requested|reviewer_verified|rejected → approved → published ↔ unpublished / update_required → archived.

Type-specific required fields enforced in `ep_validate_opportunity_payload()` and on submit.
