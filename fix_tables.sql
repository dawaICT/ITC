-- RETIRED: this emergency script previously dropped students, registrations,
-- logins, and payment ledgers before recreating only a subset of their schema.
-- It is intentionally non-mutating. Use a reviewed migration from /migrations
-- for any schema repair.

SELECT 'RETIRED: destructive table repair was not executed.' AS migration_status;

